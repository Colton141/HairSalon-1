@{
  Layout = "_Layout";
}

<h3>Here are all the Clients for this Stylist: @Model["stylist"].GetName()</h3>



  <ul>
  @foreach (var client in @Model["clients"])
  {
    <li>@client.GetName()</li>
  }
  </ul>
  <form action='/stylists/@Model["stylist"].GetId()/clients' method='post'>
    <label for='clientId'>Select a client</label>
    <select id='clientId' name='clientId' type='text'>
      @foreach (var client in @Model["allClients"])
      {
        <option value='@client.GetId()'>@client.GetName()</option>
      }
    </select>
    <button type='submit'>Add</button>
  </form>


<h4>Add a specialty to this stylist:</h4>

<ul>
  @foreach (var specialty in @Model["specialties"])
  {
    <li>@specialty.GetName()</li>
  }
</ul>

<form action='/stylists/@Model["stylist"].GetId()/specialties' method='post'>
  <label for='specialtyId'>Select a specialty</label>
  <select id='specialtyId' name='specialtyId' type='text'>
    @foreach (var specialty in @Model["allSpecialties"])
    {
      <option value='@specialty.GetId()'>@specialty.GetName()</option>
    }
  </select>
  <button type='submit'>Add</button>
</form>


<p><a href='/clients/new'>Add a New Client</a></p>
<p><a href="/specialties/new">Add a Specialty</a></p>
<p><a href='/stylists'>Return to Stylists Page</a></p>
<p><a href='/clients'>View Clients Page</a></p>
<p><a href='/specialties'>View Specialties Page</a></p>
<p><a href='/'>Return to Main Page</a></p>
